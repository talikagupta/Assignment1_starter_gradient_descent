def gradient_descent(a: float, b: float, c: float, d:float, initial_x: float, learning_rate: float, num_iters: int) -> float:
    """
    Perform gradient descent to minimize the function f(x) = ax^3 + bx^2 + cx + d.

    Parameters:
        a (float): Coefficient of x^3 in the quadratic function.
        b (float): Coefficient of x^2 in the quadratic function.
        c (float): Coefficient of x in the quadratic function.
        d (float): Constant term in the quadratic function.
        initial_x (float): Initial guess for x.
        learning_rate (float): Learning rate for gradient descent.
        num_iters (int): Number of update steps.

    Returns:
        float: The value of x that minimizes f(x).
    """
    # Your implementation here
    pass
    